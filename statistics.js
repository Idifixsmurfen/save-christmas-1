
function max(arr) 
{
    let largestValue = 0

    for (let i = 0; i < array.length; i++)
    {
    if (arr[i] > largestValue)
    {
        largestValue = arr[i]
    }
    }
    return largestValue
    console.log(largestValue)
}

function min(arr) 
{
    let smallestValue = arr[0]

    for (let i = 0; i< array.length; i++)
    {
        if (arr[i] < smallestValue)
        {
            smallestValue = arr[i]
        }
    }

    return smallestValue
}

function mean(arr) 
{
    let meanValue = arr[0]

    for (let i = 0; i< array.length; i++)
    {
        if (arr[i] < meanValue)
        {
            meanValue = arr[i]
        }
    }
    console.log(result)
    return result / arr.lenhth
}
